---
import { t } from "i18next";
import { localizePath } from "astro-i18next";
---

<a
  class="block text-center text-4xl font-bold"
  href={localizePath(import.meta.env.BASE_URL)}
>
  {t("site.nabi")}</a
>
<div class="divider mx-auto mb-1 mt-7 h-px w-[80vw] max-w-5xl bg-black"></div>
